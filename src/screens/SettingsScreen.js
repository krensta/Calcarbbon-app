import { Settings } from "react-native";
import { View, Text } from "react-native-web"

const SettingsScreen =({})=> {
    return(
        <View>
         <Text>Esto es un Settings</Text>   
        </View>
    )
} 

export default SettingsScreen;